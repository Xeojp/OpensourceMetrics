# Анализатор метрик GitHub проектов по opensource.guide

## Чтобы запустить утилиту - используйте:

```bash
# Для установки зависимостей
sudo apt install jq bc git curl

chmod +x metrics.sh

./metrics.sh torvalds/linux
./metrics.sh facebook/react
